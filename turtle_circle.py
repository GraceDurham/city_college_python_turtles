import turtle # allows us to use the turtles library
wn = turtle.Screen() # creates a graphics window
grace = turtle.Turtle() # create a turtle named grace grace.penup()
grace.penup()
grace.goto(0, -200)
grace.pendown()
grace.circle(200)
